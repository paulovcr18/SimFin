---
phase: 01-dead-code-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tracker/portfolio_tracker.py
  - tracker/portfolio_transactions.json
  - tracker/requirements.txt
  - streamlit-app/README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "tracker/ directory does not exist anywhere in the git-tracked working tree"
    - "streamlit-app/README.md exists and contains a visible deprecation notice naming index.html as the main product"
  artifacts:
    - path: "streamlit-app/README.md"
      provides: "Deprecation notice for Streamlit app"
      contains: "deprecated"
  key_links:
    - from: "streamlit-app/README.md"
      to: "index.html"
      via: "explicit link or reference in deprecation notice"
      pattern: "index\\.html"
---

<objective>
Delete the `tracker/` directory entirely and add a deprecation notice to `streamlit-app/README.md`.

Purpose: `tracker/` is 100% redundant with `streamlit-app/` — same yfinance price-fetch logic, same cache strategy, no other users. It creates a dual-maintenance burden where bug fixes must be applied in two places. Removing it eliminates the risk of divergence. The Streamlit app is kept but marked as secondary to the PWA.

Output:
- `tracker/` fully removed from the repository
- `streamlit-app/README.md` created (file does not currently exist) with deprecation notice
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dead-code-removal/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete tracker/ directory</name>
  <files>tracker/portfolio_tracker.py, tracker/portfolio_transactions.json, tracker/requirements.txt</files>
  <read_first>
    - tracker/portfolio_tracker.py (confirm it is standalone and has no reverse dependencies before deleting)
    - tracker/requirements.txt (confirm contents for the deprecation notice, if needed)
  </read_first>
  <action>
    Run: `git rm -r tracker/`

    This removes all three files tracked in git:
    - tracker/portfolio_tracker.py
    - tracker/portfolio_transactions.json
    - tracker/requirements.txt

    Do NOT delete streamlit-app/ or any other directory.

    Verify with `git status` that only the tracker/ files are staged for deletion and nothing else.
  </action>
  <verify>
    <automated>git status --short | grep tracker</automated>
  </verify>
  <done>
    `git status` shows three `D  tracker/...` lines and no other unexpected changes.
    `ls tracker/` returns "No such file or directory".
  </done>
</task>

<task type="auto">
  <name>Task 2: Create streamlit-app/README.md with deprecation notice</name>
  <files>streamlit-app/README.md</files>
  <read_first>
    - streamlit-app/app.py (to confirm the app's current purpose and name for accurate notice wording)
    - README.md (repo root — to ensure wording is consistent with main readme)
  </read_first>
  <action>
    Create `streamlit-app/README.md` with the following content (exact wording, Claude decides formatting):

    - A top-level heading: "SimFin — Streamlit App (Deprecated)"
    - A clearly visible deprecation notice (use a blockquote or bold warning) stating:
      - This Streamlit app is no longer the primary product.
      - The main product is the PWA at `index.html` in the repository root.
      - This app is kept for reference only and may be removed in a future release.
      - New features will not be added here.
    - A brief one-paragraph description of what the app does (payroll simulator + portfolio tracker, Supabase-backed, runs via `streamlit run streamlit-app/app.py`).
    - A section listing the primary product: the PWA (`index.html`), with a note that it works offline and is the actively maintained version.

    Do NOT write anything that promises this app is maintained or production-ready.
    The file must contain the word "deprecated" (case-insensitive) and reference "index.html".
  </action>
  <verify>
    <automated>grep -i "deprecated" streamlit-app/README.md && grep "index.html" streamlit-app/README.md</automated>
  </verify>
  <done>
    `streamlit-app/README.md` exists.
    `grep -i deprecated streamlit-app/README.md` returns at least one match.
    `grep index.html streamlit-app/README.md` returns at least one match.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# Confirm tracker/ is gone
ls tracker/ 2>&1 | grep "No such file"

# Confirm README exists with required content
grep -i "deprecated" streamlit-app/README.md
grep "index.html" streamlit-app/README.md

# Confirm no other files accidentally changed
git diff --name-only HEAD
```
</verification>

<success_criteria>
- `tracker/` does not exist in the working tree or git index
- `streamlit-app/README.md` exists, contains "deprecated" (any case), and references "index.html"
- `git status` shows only the expected changes: 3 deletions from tracker/ and 1 new file at streamlit-app/README.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-dead-code-removal/01-01-SUMMARY.md`
</output>
